apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: data
  labels:
    app.kubernetes.io/part-of: cloudsalaryhub
    app.kubernetes.io/component: database
  annotations:
    app.kubernetes.io/description: "PostgreSQL superuser credentials (data namespace only)"
type: Opaque
stringData:
  # -------------------------------------------------------
  # dev only — placeholder values for minikube/microk8s. Replace before production.
  # -------------------------------------------------------
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres"
