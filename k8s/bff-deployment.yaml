apiVersion: apps/v1
kind: Deployment
metadata:
  name: bff-service
  namespace: app
  labels:
    app.kubernetes.io/name: bff-service
    app.kubernetes.io/part-of: cloudsalaryhub
    app.kubernetes.io/component: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bff-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bff-service
        app.kubernetes.io/part-of: cloudsalaryhub
        app.kubernetes.io/component: gateway
    spec:
      containers:
        - name: bff-service
          # Local (microk8s / minikube) — Docker Hub image:
          # image: rajithaeyee/cloudsalaryhub-bff-service:latest
          # AWS EKS rendered by GitHub Actions via envsubst:
          image: ${ECR_REGISTRY}/cloudsalaryhub/bff-service:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 30
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
