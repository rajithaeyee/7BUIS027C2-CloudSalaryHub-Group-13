apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: cloudsalaryhub
  labels:
    app.kubernetes.io/name: search-service
    app.kubernetes.io/part-of: cloudsalaryhub
    app.kubernetes.io/component: backend
  annotations:
    app.kubernetes.io/description: "ClusterIP service for search-service"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: search-service
  ports:
    - name: http
      port: 8003
      targetPort: 8003
      protocol: TCP
