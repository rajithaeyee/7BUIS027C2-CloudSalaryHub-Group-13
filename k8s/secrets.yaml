apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: cloudsalaryhub
  labels:
    app.kubernetes.io/part-of: cloudsalaryhub
  annotations:
    app.kubernetes.io/description: "Sensitive credentials"
type: Opaque
stringData:
  # -------------------------------------------------------
  # dev only — these placeholder values are for minikube. and will be changed in prod
  # ---------------------------------------------------------
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "postgres"
  JWT_SECRET: "change-me-in-production"
  DATABASE_URL: "postgresql://postgres:postgres@postgres:5432/cloud-salary-hub"
